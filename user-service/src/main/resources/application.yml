server:
  port: 9002
  
spring:
  application:
    name: USER-SERVICE
  zipkin:
    base-url: http://localhost:9411/
    
#removing here because it getting values from config-server  
#eureka:
#  client:
#    register-with-eureka: true
#    fetch-registry: true
#    service-url:
#      defaultZone: http://localhost:8761/eureka/
#  instance:
#    hostname: localhost